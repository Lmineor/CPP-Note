/*
文件类型分为两种
1. 文本文件 文件以文体ASCII码形式存储在计算机中
2. 二进制文件  文件以文本的二进制形式存储在计算机中，用户一般不能直接读懂它们

操作文件的三大类
1. ofstream 写操作
2. ifstream 读
3. fstream  读写操作

*/

#include<fstream>

using namespace std;
int test01()
{
    // ofstream ofs;
    // ofs.open("文件路径", 打开方式);
    // ofs << " 写入的数据";
    // ofs.close();
}
int main()
{
    return 0;
}